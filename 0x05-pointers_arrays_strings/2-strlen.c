#include "main.h"
#include <stdio.h>
/**
 *_strlen - a function that returns the length of a string.
 *@s: string.
 *Return: Always 0.
 */
int _strlen(char *s)
{
int len;
for (len = 0; s[len] != '\0'; ++len);
return (0);
}
