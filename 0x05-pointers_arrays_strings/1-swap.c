#include "main.h"
#include <stdio.h>
/**
 *swap_int-swaps the values of two integers.
 *@a:integer a.
 *@b:integer b.
 *@k:integer value.
 *return: always 0.
 */
void swap_int(int *a, int *b)
{
  int k = *a;
     *a = *b
     *b = k
  return (0);
}
